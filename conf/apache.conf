<Directory /var/www>
    <IfModule mod_rewrite.c>
      Options +symlinksifownermatch
      RewriteEngine On
      RewriteBase /
      RewriteCond %{request_filename} !-f
      RewriteCond %{request_filename} !-d
      RewriteRule ^(lintory(/.*)?)$ /lintory.fcgi/$1 [l,qsa]
    </IfModule>
</Directory>

Alias /media /usr/share/spud/media
Alias /admin_media /usr/share/pyshared/django/contrib/admin/media

FastCGIExternalServer /var/www/lintory.fcgi -socket /var/run/lintory/inventory.socket
